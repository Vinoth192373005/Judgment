version: '3.8'

services:
  judgment:
    build: .
    container_name: judgment-ai-service
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
